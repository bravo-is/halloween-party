{
  "name": "after-dark",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev --host 0.0.0.0",
    "dev:full": "netlify dev",
    "build": "astro build",
    "preview": "astro preview --host 0.0.0.0",
    "test": "node --test tests/*.test.mjs",
    "social:generate": "node scripts/generate-social-card.mjs",
    "sigil:generate": "node scripts/generate-sigil.mjs"
  },
  "dependencies": {
    "@netlify/blobs": "^10.0.0",
    "astro": "^7.3.2",
    "qrcode": "^1.5.4"
  },
  "devDependencies": {
    "netlify-cli": "^27.6.0",
    "sharp": "^0.35.4"
  }
}
